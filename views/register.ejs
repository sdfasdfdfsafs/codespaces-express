<!DOCTYPE html>

<html>
    <head>
        <title>Site</title>
        <link rel="stylesheet" href="styles.css" />
        <link rel="icon" href="favicon.ico" type="image/x-icon" />
        <script type="text/javascript" src="script.js"></script>
    </head>

    <body>
        <!-- Add HTML markup elements for your web site below -->
        <h2 name = "title">Register Here!</h2>
        
        <div>
        <% if(free) { %>
            <form action="/register" method="post">
                <div><input type = "text" name = "name" placeholder = "name"></div>
                <br/>
                <div><input type = "text" name = "email" placeholder = "email"></div>
                
                <br/>
                <div><input type = "password" name = "password" placeholder = "password"></div>

                <br/>
                <% if(error) { %>
                    <div class = "again" id = "yes" style = "width: 70%;">Invalid Email or Password</div>
                <% } %>
                <br/>
                <div><input type="submit" value="register"/></div>
    
            </form>
        <% } %>

        <br>
        <% if(code) { %>
            <form action="/code" method="post">
                <div><input type = "text" name = "code" placeholder = "code"></div>
                <br/>
                <div><input type="submit" value="code"/></div>
    
            </form>
        <% } %>
        <br/>
        </div>

    </body>
</html>
